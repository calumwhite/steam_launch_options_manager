script to run my program and then launch steam
run everytime to  advoid issues
uses config in $XDG_CONFIG_HOME defaulting to $HOME/.config

config should contain location of steam config
rules

initialiser should help find the config
and help set rules

backup should be done to $XDG_DATA_HOME or  $HOME/.local/share
